# Critical Rules

- Never commit secrets/keys to repository
- Use environment variables for all secrets
- Always run lint -> build before committing
- Follow branch naming: `feature/{scope}-{id}`, `fix/{scope}-{id}`, `chore/{scope}-{id}`
- Use Conventional Commits: `feat(scope):`, `fix(scope):`, `refactor(scope):`, `docs(scope):`